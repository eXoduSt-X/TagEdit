<resources>
    <!-- Theme.MaterialComponents (sin ".Light") es oscuro por default, así que forzamos
         gris oscuro siempre, independiente del modo claro/oscuro del sistema. -->
    <style name="Theme.TagEdit" parent="Theme.MaterialComponents.NoActionBar">
        <item name="android:windowBackground">@color/app_background</item>
        <item name="android:colorBackground">@color/app_background</item>

        <!-- colorSecondary/colorControlActivated es lo que pinta checkboxes, switches,
             el cursor y el subrayado de los EditText cuando están "activados". -->
        <item name="colorPrimary">@color/electric_blue</item>
        <item name="colorPrimaryVariant">@color/electric_blue</item>
        <item name="colorSecondary">@color/electric_blue</item>
        <item name="colorSecondaryVariant">@color/electric_blue</item>
        <item name="colorControlActivated">@color/electric_blue</item>
        <item name="colorOnPrimary">@color/text_white</item>
        <item name="colorOnSecondary">@color/text_white</item>

        <item name="android:textColorPrimary">@color/text_white</item>
        <item name="android:textColorSecondary">@color/text_secondary</item>

        <!-- Bajo MaterialComponents, <Button> en XML se infla como MaterialButton, así que
             el estilo por defecto de los botones se pisa acá (no con android:buttonStyle). -->
        <item name="materialButtonStyle">@style/Widget.TagEdit.Button</item>
    </style>

    <style name="Widget.TagEdit.Button" parent="Widget.MaterialComponents.Button.UnelevatedButton">
        <item name="backgroundTint">@color/button_background</item>
        <item name="rippleColor">@color/button_background</item>
        <item name="android:textColor">@color/text_white</item>
    </style>

    <style name="Widget.TagEdit.Button.Small" parent="Widget.TagEdit.Button">
        <item name="android:minWidth">0dp</item>
        <item name="android:minHeight">0dp</item>
        <item name="android:textAllCaps">false</item>
        <item name="android:textSize">13sp</item>
        <item name="android:paddingStart">12dp</item>
        <item name="android:paddingEnd">12dp</item>
        <item name="android:paddingTop">6dp</item>
        <item name="android:paddingBottom">6dp</item>
        <item name="cornerRadius">6dp</item>
    </style>
</resources>
